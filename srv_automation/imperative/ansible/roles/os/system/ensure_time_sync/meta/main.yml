---
galaxy_info:
  role_name: ensure_time_sync
  description: Ensure reliable system time synchronization (systemd-timesyncd + timedatectl NTP)
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: ["jammy"]
dependencies: []

