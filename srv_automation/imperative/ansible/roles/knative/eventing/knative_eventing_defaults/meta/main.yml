---
galaxy_info:
  role_name: knative_eventing_defaults
  description: Create/upgrade a Broker, a catch-all Trigger to a receiver, and PingSources for schedules; verify Ready.
  min_ansible_version: "2.14"
  license: Apache-2.0
  platforms:
    - name: Ubuntu
      versions: ["jammy"]

dependencies: []

